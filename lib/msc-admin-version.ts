/**
 * Shown in Payload admin (sidebar) so you can confirm a deploy landed.
 * Bump **patch** (1.0.0 -> 1.0.1) when you ship admin UI, `payload.config`,
 * import map, middleware that affects `/admin`, or other CMS-panel changes.
 */
export const MSC_ADMIN_VERSION = "1.0.24"
