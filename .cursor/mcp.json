{
  "mcpServers": {
    "local-wp": {
      "command": "cmd",
      "args": ["/c", "npx", "-y", "@verygoodplugins/mcp-local-wp@latest"],
      "_comment": "Local by Flywheel / Local WP site discovery for mscclean.local"
    },
    "mcp-wordpress": {
      "command": "cmd",
      "args": [
        "/c",
        "node",
        "C:\\Users\\JONBEATZ\\AppData\\Roaming\\npm\\node_modules\\mcp-wordpress\\dist\\index.js"
      ],
      "env": {
        "WORDPRESS_SITE_URL": "REPLACE_WITH_WP_SITE_URL",
        "WORDPRESS_USERNAME": "REPLACE_WITH_WP_USERNAME",
        "WORDPRESS_APP_PASSWORD": "REPLACE_WITH_WP_APP_PASSWORD"
      },
      "_comment": "WordPress REST MCP — credentials synced from .env.local via npm run sync:mcp-env"
    },
    "browsermcp": {
      "command": "cmd",
      "args": ["/c", "npx", "-y", "@browsermcp/mcp@latest"],
      "_comment": "Allows Cursor/Claude to control your actual open Chrome/Edge browser window locally."
    },
    "browserbase": {
      "command": "cmd",
      "args": ["/c", "npx", "-y", "@browserbasehq/mcp"],
      "env": {
        "BROWSERBASE_API_KEY": "REPLACE_WITH_BROWSERBASE_API_KEY",
        "BROWSERBASE_PROJECT_ID": "REPLACE_WITH_BROWSERBASE_PROJECT_ID",
        "GEMINI_API_KEY": "REPLACE_WITH_GEMINI_API_KEY"
      },
      "_comment": "Cloud browser automation with Browserbase and Stagehand."
    },
    "21st-dev-magic": {
      "command": "cmd",
      "args": ["/c", "npx", "-y", "@21st-dev/magic@latest"],
      "env": {
        "API_KEY": "REPLACE_WITH_21ST_DEV_MAGIC_API_KEY"
      },
      "_comment": "Allows building beautiful frontend components directly into the codebase with 21st.dev Magic MCP."
    },
    "markdownify": {
      "command": "cmd",
      "args": ["/c", "npx", "-y", "mcp-markdownify-server"],
      "_comment": "Converts web content, PDFs, images (OCR), and document files directly to Markdown."
    }
  }
}
